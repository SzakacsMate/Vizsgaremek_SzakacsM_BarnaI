export type Session = {
  id: number;
  title: string;
  system: string;
  sessionNumber: number;
  date: string;
  duration: string;
  location: string;
  playerLimit: number;
  players: string[];
  systemColor: string;
};